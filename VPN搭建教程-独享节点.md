# 搭建专属独享 VPN 节点（手把手新手版）

> 方案：**VLESS + Reality**（自用最佳，抗封锁强、不用域名、不用证书）
> 适用：完全新手 · Windows 优先 · 之后 Mac M3
> 目标：**只有你自己能用的独享节点**

---

## 总览：整个过程分几步

1. 买一台海外 VPS 服务器（唯一花钱的地方）
2. 在 Windows 上用 SSH 登录这台服务器
3. 跑一条一键脚本，自动搭好节点（生成 `vless://` 链接 + 二维码）
4. 把链接导入 **Windows** 客户端，连上
5. 在 **Mac M3** 上导入同一条链接
6. 在 **iPhone** 上导入同一条链接
7. 在 **Android** 上导入同一条链接

> 核心：**只需搭建一次**，生成的同一条 `vless://` 链接（或二维码）可以在你所有设备上重复使用，无需重复搭建。

预计时间：**30~40 分钟**。花钱：服务器约 ¥30~50/月。

---

## 第 1 步：购买海外 VPS 服务器（用 Vultr，新手最稳）

为什么第一台选 **Vultr**：
- 支持**支付宝 / 微信 / 银联**付款，不用国外信用卡
- **按小时计费**，搭坏了删掉重开只花几毛钱，零压力试错
- 即开即用，1 分钟就给你服务器

> ⚠️ 不要买「机场 / 订阅 / 共享节点」——那就是你说的"公共的"。自己租 VPS 才是真正独享。

---

### 1-A. 注册账号

1. 打开官网：https://www.vultr.com
2. 点右上角 **Sign Up**（注册）
3. 填邮箱 + 设置密码 → 提交
4. 去你的邮箱点**验证邮件里的链接**，激活账号

### 1-B. 充值（先充 $10）

1. 登录后，进入 **Billing（账单）** 页面
2. 选付款方式：
   - **Alipay（支付宝）** 或 **WeChat Pay（微信）** ← 选这个，扫码付款
   - 也可以用 PayPal
3. 充值金额选 **$10**（够用好几个月）

> Vultr 有时新用户充值有赠送活动，按页面提示走即可。

### 1-C. 创建服务器（关键，每个选项照着选）

1. 点左侧 **Products** → 右上角蓝色 **加号 (+)** → **Deploy New Server**
2. 按下面这张表逐项选：

| 选项 | 选什么 | 说明 |
|---|---|---|
| **Choose Type** | **Cloud Compute - Shared CPU** | 最便宜，自用足够 |
| **CPU & Storage Technology** | **Regular Performance**（或 AMD/Intel 高性能任选） | 普通的就行，更便宜 |
| **Server Location** | **Tokyo（东京）** 首选；或 Seoul/Singapore | 离中国近、速度快 |
| **Server Image (系统)** | **Debian 12 x64** | 照教程必须选这个 |
| **Server Size** | 最低档 **$5~6/月**（1 vCPU / 1GB 内存 / 25GB） | 够用 |
| **Auto Backups** | **关掉**（点掉勾） | 省钱，自用不需要 |
| **网络 / IPv4** | **不用改，保持默认即可** | 见下方说明 |
| **Server Hostname & Label** | 随便填，比如 `myvpn` | 自己认得就行 |

> **关于「Additional Features」找不到？**  
> Vultr 近年改版了界面，旧版里的 **Additional Features** 可能：
> - 改名为 **Instance Connectivity（实例连接方式）**，在填完 Hostname 后、点 Deploy 前的页面底部；
> - 或整段被收进 **Configure Software / Server Settings** 第二步，需要往下滚才看到；
> - 甚至**完全不显示**——因为默认就是带公网 IPv4 的普通实例，无需手动勾选。
>
> **你只需要确认两件事：**
> 1. 连接方式选 **Instance(s) with Public IP**（带公网 IP 的实例），**不要**选 Private behind NAT Gateway；
> 2. **Public IPv4 保持开启**（默认就是开的，别手动关掉）。
>
> 如果页面上根本没有这些选项，**直接忽略，继续 Deploy 即可**——按本教程选的 Cloud Compute 默认就会分配 IPv4。

3. 右侧会显示价格，确认是最低档，点最下方 **Deploy Now**（或 **Deploy**）

### 1-D. 拿到服务器信息（一定记下来）

1. 等约 1~2 分钟，服务器状态从 `Installing` 变成 **`Running`**
2. 点进这台服务器，在 **Overview / Server Information** 里能看到：

```
IP Address（IP 地址）：例如 123.45.67.89
Username（用户名）：root
Password（密码）：点旁边的小眼睛👁图标显示，复制出来
```

3. **把这三样复制到记事本存好**——第 2 步登录要用。

---

## 第 2 步：用 Windows 登录服务器（SSH）

Windows 10/11 自带工具，**不用装任何东西**。

1. 按键盘 `Win + R`，输入 `cmd`，回车，打开黑色命令行窗口
2. 输入下面这行（把 IP 换成你自己的）：

```bash
ssh root@123.45.67.89
```

3. 第一次会问 `Are you sure you want to continue connecting (yes/no)?` → 输入 `yes` 回车
4. 提示 `password:` → 输入你的密码（**注意：输密码时屏幕不显示任何字符，是正常的**），回车
5. 看到类似 `root@xxx:~#` 的提示符，就代表**登录成功了**

---

## 第 3 步：一键搭建节点（核心步骤）

登录成功后，**复制下面这一整行**，粘贴到窗口里回车（在命令行里粘贴：点窗口左上角图标 → 编辑 → 粘贴，或直接右键）：

```bash
bash <(curl -Ls https://raw.githubusercontent.com/233boy/sing-box/main/install.sh)
```

> 这是知名开源项目 [233boy/sing-box](https://github.com/233boy/sing-box) 的一键脚本，会自动帮你装好并默认生成一个 VLESS+Reality 节点。

脚本运行后：

- 它会自动安装、配置、生成节点
- 结束时会**直接显示一条以 `vless://` 开头的链接**，以及一张二维码
- **把那条 `vless://...` 链接完整复制出来，存到记事本**——这就是你的专属节点，别外传

### 常用管理命令（记一下）

```bash
sb        # 打开管理菜单（增删节点、查看链接、改端口等）
```

---

## 第 4 步：Windows 客户端连接

### 下载客户端

推荐用 **v2rayN**（免费开源）：

- 下载地址：https://github.com/2dust/v2rayN/releases
- 下载文件名里带 `windows` 和 `with-core` 的那个 zip，解压后双击 `v2rayN.exe`

### 导入你的节点

1. 先复制你第 3 步存下的那条 `vless://...` 链接
2. 打开 v2rayN → 顶部菜单 **服务器** → **从剪贴板导入批量URL**
3. 列表里会出现你的节点
4. 右下角托盘图标右键 → **系统代理** → 选 **自动配置系统代理（PAC）** 或 **全局**
5. 完成！打开浏览器测试是否能访问外网

---

## 第 5 步：Mac M3 上使用（以后用得到）

同一个节点链接，Mac 上也能用，**不用重新搭建**。

1. 下载 **ClashX Meta** 或 **V2rayU**（GitHub 搜索下载）
   - 推荐 ClashX Meta：https://github.com/MetaCubeX/ClashX.Meta/releases
2. 复制同一条 `vless://...` 链接
3. 在客户端里选「从剪贴板导入 / 扫码导入」
4. 选中节点，开启代理即可

> 苹果 M 芯片选 Apple Silicon / arm64 版本的客户端。

---

## 第 6 步：iPhone 上使用

同一条 `vless://...` 链接，iPhone 也能直接用，**不用重新搭建**。

### 下载客户端（任选一个）

iPhone 上这类 App 在**国区 App Store 搜不到**，需要用**非中国区（美区）的 Apple ID** 下载：

| App | 是否收费 | 说明 |
|---|---|---|
| **Shadowrocket（小火箭）** | 约 $2.99 | 最经典好用，**首选** |
| **Stash** | 收费 | 功能强，界面好 |
| **Sing-box（官方）** | 免费 | 和服务端同款，免费可用 |

> 没有美区 ID？可以新注册一个地区为美国的 Apple ID（无需美国信用卡，付款方式选"无 / None"即可下免费的 Sing-box；买小火箭则需要美区余额）。

### 导入节点（以小火箭为例）

**方法 A：扫码导入（最简单）**
1. 让电脑/服务器显示那张节点二维码（第 3 步脚本结束时有，或在服务器里输 `sb` 重新查看）
2. 打开小火箭 → 右上角 **扫一扫（+ 号旁的扫码图标）** → 扫二维码
3. 节点自动添加

**方法 B：粘贴链接导入**
1. 把 `vless://...` 链接复制到 iPhone 剪贴板（可用微信/备忘录传给自己）
2. 打开小火箭 → 右上角 **+** → 类型选 **Subscribe / 手动粘贴**，或它会提示"检测到剪贴板有节点，是否添加"→ 点添加

### 连接
1. 回到小火箭首页，选中你的节点
2. 把顶部开关从 **「不连接」** 拨到 **「全局路由 / 配置」**
3. 第一次会弹出**添加 VPN 配置**的系统授权 → 点**允许**，输入手机密码
4. 顶部出现 **VPN** 小图标 = 连接成功，打开浏览器测试

---

## 第 7 步：Android 安卓机上使用

同一条链接，安卓也能用，且客户端**免费、无需特殊账号**。

### 下载客户端（任选一个）

| App | 说明 | 下载 |
|---|---|---|
| **v2rayNG** | 最主流，**首选** | GitHub: https://github.com/2dust/v2rayNG/releases |
| **NekoBox** | 功能全，支持协议多 | GitHub: https://github.com/MatsuriDayo/NekoBoxForAndroid/releases |
| **Sing-box** | 官方同款 | 应用商店或 GitHub |

> 下载 GitHub 里带 `arm64-v8a` 的 `.apk` 文件，手机点开安装（若提示"未知来源"，按提示允许安装即可）。

### 导入节点（以 v2rayNG 为例）

**方法 A：扫码导入**
1. 打开 v2rayNG → 右上角 **+** → **扫描二维码**
2. 扫第 3 步生成的节点二维码

**方法 B：剪贴板导入**
1. 复制 `vless://...` 链接
2. 打开 v2rayNG → 右上角 **+** → **从剪贴板导入**

### 连接
1. 选中列表里的节点
2. 点右下角 **V 形（▶）启动按钮**
3. 第一次弹出**网络连接请求**授权 → 点**允许**
4. 按钮变绿、状态栏出现钥匙图标 = 连接成功，打开浏览器测试

---

## 关于"独享 / 不公共"——你已经做到了

你这套节点天然就是独享的，原因：

1. **服务器是你一个人租的**，没有别人在上面
2. **节点链接里的 UUID 是唯一密钥**，只在你手里，不发别人就没人能连
3. **没有共享 IP**，整条线路只有你在跑

### 想再安全一点（可选）

- 不要把 `vless://` 链接发到任何群、网盘、聊天里
- 服务器密码设复杂一些，或登录后改用密钥登录
- 用 `sb` 菜单可以随时换端口 / 重新生成节点

---

## 出问题怎么办（速查）

| 现象 | 解决 |
|---|---|
| SSH 连不上 | 检查 IP 有没有写错、服务器是否已开机（后台看状态）|
| 脚本下载失败 | 多试几次，或换 `curl` 为 `wget`，详见脚本项目主页 |
| 客户端连上但上不了网 | 检查是否开了"系统代理 / 全局模式"，换个节点端口 |
| 速度慢 | 换机房（日本/香港/新加坡），或换更好线路的服务商 |

---

## 费用小结

| 项目 | 费用 |
|---|---|
| VPS 服务器 | ¥30~50/月（唯一必须花的）|
| 客户端软件 | 免费 |
| 域名 | 不需要（Reality 方案免域名）|

---

有任何一步卡住了，把报错信息发我，我帮你看。
